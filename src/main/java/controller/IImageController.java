package controller;

/**
 * An interface that represents a controller for the commands that can be run on an ImageModel which
 * are provided by the user.
 */
public interface IImageController {

  /**
   * Runs the commands provided by the user on the Image Model.
   */
  void goCommands();

}
